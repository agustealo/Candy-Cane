# Candy Cane documentation

This directory contains documentation-only assets and project material. It is excluded from the installable WordPress theme package.

The `screenshots/` directory is populated only with captures from the real deterministic WordPress showcase runtime. Generated browser screenshots are never substituted with mockups.
